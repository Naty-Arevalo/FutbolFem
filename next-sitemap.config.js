/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://futbol-fem.vercel.app/', // Cambia por tu dominio real
    generateRobotsTxt: true,
    outDir: './public', // Muy importante para que aparezca en producción
  };
  